package algo;

import java.util.HashSet;
import java.util.Set;

public class Substring2solu {
    public static void main(String s){
        int x = 0;
//        Map<String, Integer> table = new HashMap<>();
//        for(x=0; x<s.length(); x++){
//            String element = ""+s.charAt(x);
//            boolean elementPresentOrNot = table.containsKey(element);
//            if(elementPresentOrNot==false){
//                table.put(element, 1);
//            }else {
//                int present = table.get(element);
//                table.put(element, present+1);
//            }
//        }
//        System.out.println(""+table);
        Set<String> table =new HashSet<>();
                table.iterator();
            String element = ""+s.charAt(x);
            boolean elementPresentOrNot = table.contains(element);

           }

    }

